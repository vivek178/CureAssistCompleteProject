import { Detailedappointments } from './detailedappointments';

describe('Detailedappointments', () => {
  it('should create an instance', () => {
    expect(new Detailedappointments()).toBeTruthy();
  });
});
